{{>header}}
{{>navbar}}
<section class="container my-3">
    <table class="table table-stripped">
        <thead>
            <tr>
                <th>id</th>
                <th>name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
{{#each data}}
            <tr>
                <td>{{this.id}}</td>
                <td>{{this.name}}</td>
                <td>
                    <a href="single/{{this.id}}" class="btn btn-primary m-1">show single</a>
                    <a href="editUser/{{this.id}}" class="btn btn-warning m-1">Edit</a>
                    <a href="del/{{this.id}}" class="btn btn-danger m-1">Delete</a>
                </td>
            </tr>
{{/each}}
        </tbody>
    </table>
</section>
{{>footer}}